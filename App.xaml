<Application x:Class="SistemaInventario.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             StartupUri="MainWindow.xaml">
    <Application.Resources>

        <!-- PALETA GENERAL -->
        <SolidColorBrush x:Key="BgAppBrush" Color="#DDEBEC"/>
        <SolidColorBrush x:Key="SidebarBrush" Color="#F4F5F2"/>
        <SolidColorBrush x:Key="HeaderBrush" Color="#F8FAFA"/>
        <SolidColorBrush x:Key="BorderSoftBrush" Color="#C9D9D9"/>

        <SolidColorBrush x:Key="TextPrimaryBrush" Color="#244855"/>
        <SolidColorBrush x:Key="TextSecondaryBrush" Color="#5C6B73"/>
        <SolidColorBrush x:Key="TextAccentBrush" Color="#6AA3A1"/>

        <!-- TARJETAS -->
        <SolidColorBrush x:Key="CardSellBrush" Color="#A9D2D6"/>
        <SolidColorBrush x:Key="CardProductsBrush" Color="#C7965B"/>
        <SolidColorBrush x:Key="CardInventoryBrush" Color="#7FA37D"/>
        <SolidColorBrush x:Key="CardReportsBrush" Color="#9EB4C8"/>

        <!-- SOMBRAS / RELIEVE -->
        <SolidColorBrush x:Key="CardSellShadowBrush" Color="#5B8D92"/>
        <SolidColorBrush x:Key="CardProductsShadowBrush" Color="#996736"/>
        <SolidColorBrush x:Key="CardInventoryShadowBrush" Color="#446B43"/>
        <SolidColorBrush x:Key="CardReportsShadowBrush" Color="#6D879C"/>

        <!-- BOTONES SIDEBAR -->
        <SolidColorBrush x:Key="SideButtonNormalBrush" Color="#DDEBEC"/>
        <SolidColorBrush x:Key="SideButtonHoverBrush" Color="#C6DDDE"/>
        <SolidColorBrush x:Key="SideButtonPressedBrush" Color="#9EC1C3"/>

        <SolidColorBrush x:Key="DangerSoftBrush" Color="#D18663"/>
        <SolidColorBrush x:Key="DangerBorderBrush" Color="#A85F41"/>
        <SolidColorBrush x:Key="WhiteSoftBrush" Color="#FFFFFF"/>

        <!-- TARJETAS BLANCAS -->
        <Style x:Key="SoftCardStyle" TargetType="Border">
            <Setter Property="Background" Value="{StaticResource HeaderBrush}"/>
            <Setter Property="CornerRadius" Value="24"/>
            <Setter Property="Padding" Value="20"/>
            <Setter Property="BorderBrush" Value="{StaticResource BorderSoftBrush}"/>
            <Setter Property="BorderThickness" Value="1"/>
        </Style>

        <!-- BOTONES DEL MENÚ LATERAL -->
        <Style x:Key="SidebarButtonStyle" TargetType="Button">
            <Setter Property="Height" Value="45"/>
            <Setter Property="Margin" Value="0,0,0,12"/>
            <Setter Property="Background" Value="{StaticResource SideButtonNormalBrush}"/>
            <Setter Property="Foreground" Value="{StaticResource TextPrimaryBrush}"/>
            <Setter Property="BorderBrush" Value="{StaticResource BorderSoftBrush}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="FontSize" Value="15"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="Bd"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="14"
                                Padding="14,8">
                            <ContentPresenter HorizontalAlignment="Center"
                                              VerticalAlignment="Center"/>
                        </Border>

                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Bd" Property="Background" Value="{StaticResource SideButtonHoverBrush}"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="Bd" Property="Background" Value="{StaticResource SideButtonPressedBrush}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="SidebarDangerButtonStyle"
               TargetType="Button"
               BasedOn="{StaticResource SidebarButtonStyle}">
            <Setter Property="Background" Value="{StaticResource DangerSoftBrush}"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderBrush" Value="{StaticResource DangerBorderBrush}"/>
        </Style>

    </Application.Resources>
</Application>